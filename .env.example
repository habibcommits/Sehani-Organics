# Sehani Organics - Environment Variables Example
# Copy this file to .env and fill in your actual values
# NEVER commit .env files to Git!

# ============================================
# BACKEND ENVIRONMENT VARIABLES
# ============================================

# MongoDB Connection String
# - Local: mongodb://localhost:27017/sehani-organics
# - Atlas: mongodb+srv://username:password@cluster.mongodb.net/sehani-organics
MONGODB_URI=your_mongodb_atlas_connection_string

# JWT Secret for authentication (minimum 32 characters)
# Generate new secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_jwt_secret_min_32_characters

# Server Configuration
PORT=3000
NODE_ENV=development

# ============================================
# DEPLOYMENT NOTES
# ============================================
# For Vercel deployment, add these environment variables in:
# Vercel Dashboard → Settings → Environment Variables
#
# Required variables:
# - MONGODB_URI (your MongoDB Atlas connection string)
# - JWT_SECRET (same secret used in development)
# - NODE_ENV=production
