# MongoDB Connection
# For local development: mongodb://localhost:27017/sehani-organics
# For production (MongoDB Atlas): mongodb+srv://username:password@cluster.mongodb.net/sehani-organics
MONGODB_URI=your_mongodb_connection_string_here

# JWT Secret (minimum 32 characters for security)
# Generate using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_jwt_secret_here_min_32_characters

# Server Port
PORT=3000

# Node Environment (development, production, test)
NODE_ENV=development
